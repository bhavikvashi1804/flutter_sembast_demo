name: flutter_sembast_demo
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  sembast: ^2.4.4+3
  flutter_bloc: ^4.0.1
  equatable: ^1.2.0
  path_provider: ^1.6.10

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
